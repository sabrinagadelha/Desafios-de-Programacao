#include<iostream>
using namespace std;

int gcd(int x, int y){
    return y == 0 ? x:gcd(y, x%y);
}

int main(){
    int numero1, numero2;
    cin >> numero1;
    cin >> numero2;
    cout << gcd(numero1, numero2) << endl;
}
